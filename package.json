{
  "name": "orbita-fortune",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "validate": "node scripts-validate.mjs",
    "check": "npm run test && npm run validate"
  }
}
